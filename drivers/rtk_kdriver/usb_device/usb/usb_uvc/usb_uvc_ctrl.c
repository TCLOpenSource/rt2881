#if 0
#include <string.h>
#include "rtk_kdriver/rtd_types.h"
#include "rtk_kdriver/pm_printk.h"
#include "usb_uvc_ctrl.h"

PRAGMA_CODESEG(BANK41);
PRAGMA_CONSTSEG(BANK41);

#if 0
UINT8 usb_uvc_in_enable(void)
{
    pm_printk(LOGGER_ERROR, "[UVC] usb_uvc_in_enable\n");
    return 0;
}

UINT8 usb_uvc_in_disable(void)
{
    pm_printk(LOGGER_ERROR, "[UVC] usb_uvc_in_disable\n");
    return 0;
}
#endif
#endif
